<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Ladybug🐞 APIs - YouTube, Music & Media APIs</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            min-height: 100vh;
            padding: 20px;
        }
        
        .container {
            max-width: 1200px;
            margin: 0 auto;
        }
        
        .header {
            background: white;
            border-radius: 20px;
            padding: 40px;
            text-align: center;
            box-shadow: 0 20px 60px rgba(0,0,0,0.3);
            margin-bottom: 30px;
        }
        
        .header h1 {
            font-size: 2.5em;
            color: #667eea;
            margin-bottom: 10px;
        }
        
        .header p {
            color: #6c757d;
            font-size: 1.1em;
        }
        
        .stats {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
            gap: 20px;
            margin: 20px 0;
        }
        
        .stat-card {
            background: white;
            padding: 30px;
            border-radius: 15px;
            text-align: center;
            box-shadow: 0 10px 30px rgba(0,0,0,0.2);
            transition: transform 0.3s;
        }
        
        .stat-card:hover {
            transform: translateY(-5px);
        }
        
        .stat-card h2 {
            color: #667eea;
            font-size: 2.5em;
            margin-bottom: 10px;
        }
        
        .stat-card p {
            color: #6c757d;
            font-size: 1em;
        }
        
        .api-categories {
            display: grid;
            gap: 20px;
            margin-top: 30px;
        }
        
        .category-card {
            background: white;
            border-radius: 15px;
            padding: 30px;
            box-shadow: 0 10px 30px rgba(0,0,0,0.2);
        }
        
        .category-header {
            display: flex;
            align-items: center;
            margin-bottom: 20px;
            padding-bottom: 15px;
            border-bottom: 2px solid #f0f0f0;
        }
        
        .category-icon {
            font-size: 2em;
            margin-right: 15px;
        }
        
        .category-title h3 {
            color: #667eea;
            font-size: 1.5em;
            margin-bottom: 5px;
        }
        
        .category-title p {
            color: #6c757d;
            font-size: 0.9em;
        }
        
        .api-list {
            display: grid;
            gap: 15px;
        }
        
        .api-item {
            background: #f8f9fa;
            padding: 15px 20px;
            border-radius: 10px;
            border-left: 4px solid #667eea;
            transition: all 0.3s;
        }
        
        .api-item:hover {
            background: #e9ecef;
            transform: translateX(5px);
        }
        
        .api-item-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 8px;
        }
        
        .api-name {
            font-weight: 600;
            color: #495057;
            font-size: 1.1em;
        }
        
        .api-path {
            color: #667eea;
            font-family: 'Courier New', monospace;
            font-size: 0.9em;
            background: white;
            padding: 4px 10px;
            border-radius: 5px;
        }
        
        .api-description {
            color: #6c757d;
            font-size: 0.9em;
            margin-bottom: 8px;
        }
        
        .api-methods {
            display: flex;
            gap: 8px;
            flex-wrap: wrap;
        }
        
        .method-badge {
            padding: 4px 12px;
            border-radius: 5px;
            font-size: 0.75em;
            font-weight: 600;
        }
        
        .method-get {
            background: #d4edda;
            color: #155724;
        }
        
        .method-post {
            background: #cce5ff;
            color: #004085;
        }
        
        .quick-links {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
            gap: 15px;
            margin-top: 30px;
        }
        
        .quick-link {
            background: white;
            padding: 20px;
            border-radius: 10px;
            text-align: center;
            text-decoration: none;
            color: #495057;
            box-shadow: 0 5px 15px rgba(0,0,0,0.2);
            transition: all 0.3s;
        }
        
        .quick-link:hover {
            transform: translateY(-5px);
            box-shadow: 0 10px 25px rgba(0,0,0,0.3);
        }
        
        .quick-link-icon {
            font-size: 2.5em;
            margin-bottom: 10px;
        }
        
        .quick-link-title {
            font-weight: 600;
            color: #667eea;
        }
        
        .footer {
            background: white;
            border-radius: 15px;
            padding: 30px;
            margin-top: 30px;
            text-align: center;
            box-shadow: 0 10px 30px rgba(0,0,0,0.2);
        }
        
        .footer-links {
            display: flex;
            justify-content: center;
            gap: 20px;
            margin-top: 15px;
            flex-wrap: wrap;
        }
        
        .footer-link {
            color: #667eea;
            text-decoration: none;
            font-weight: 600;
            transition: color 0.3s;
        }
        
        .footer-link:hover {
            color: #764ba2;
        }
        
        .loading {
            text-align: center;
            padding: 20px;
            color: #6c757d;
        }
        
        @media (max-width: 768px) {
            .header h1 {
                font-size: 1.8em;
            }
            
            .stat-card h2 {
                font-size: 2em;
            }
            
            .api-item-header {
                flex-direction: column;
                align-items: flex-start;
                gap: 8px;
            }
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <h1>🐞 Ladybug APIs</h1>
            <p>Powerful API Server with YouTube, Music & Media Processing</p>
        </div>
        
        <div class="stats">
            <div class="stat-card">
                <h2 id="totalRoutes">--</h2>
                <p>Total Routes</p>
            </div>
            <div class="stat-card">
                <h2 id="uptime">--</h2>
                <p>Uptime</p>
            </div>
            <div class="stat-card">
                <h2 id="version">--</h2>
                <p>Version</p>
            </div>
            <div class="stat-card">
                <h2 id="status">--</h2>
                <p>Status</p>
            </div>
        </div>
        
        <div class="api-categories" id="apiCategories">
            <div class="loading">Loading API categories...</div>
        </div>
        
        <div class="quick-links">
            <a href="/api/status" class="quick-link">
                <div class="quick-link-icon">📊</div>
                <div class="quick-link-title">API Status</div>
            </a>
            <a href="/api/routes" class="quick-link">
                <div class="quick-link-icon">📋</div>
                <div class="quick-link-title">All Routes</div>
            </a>
            <a href="/api/settings" class="quick-link">
                <div class="quick-link-icon">⚙️</div>
                <div class="quick-link-title">Settings</div>
            </a>
            <a href="/api/info" class="quick-link">
                <div class="quick-link-icon">ℹ️</div>
                <div class="quick-link-title">API Info</div>
            </a>
        </div>
        
        <div class="footer">
            <p><strong>Ladybug🐞 APIs v2.0.0</strong></p>
            <p>Created by <strong>Ntando Mods</strong> | Powered by Express.js</p>
            <div class="footer-links">
                <a href="/api/status" class="footer-link">Status</a>
                <a href="/api/routes" class="footer-link">Documentation</a>
                <a href="https://github.com" class="footer-link">GitHub</a>
                <a href="mailto:support@ladybugapi.com" class="footer-link">Support</a>
            </div>
        </div>
    </div>
    
    <script>
        // Load API stats
        fetch('/api/status')
            .then(res => res.json())
            .then(data => {
                document.getElementById('totalRoutes').textContent = data.totalRoutes || '--';
                document.getElementById('uptime').textContent = formatUptime(data.uptime);
                document.getElementById('version').textContent = data.version || 'v2.0.0';
                document.getElementById('status').textContent = '🟢 Online';
            })
            .catch(err => {
                console.error('Failed to load stats:', err);
                document.getElementById('status').textContent = '🔴 Error';
            });
        
        // Load API categories
        fetch('/api/settings')
            .then(res => res.json())
            .then(data => {
                const categories = data.data.categories || [];
                displayCategories(categories);
            })
            .catch(err => {
                console.error('Failed to load categories:', err);
                document.getElementById('apiCategories').innerHTML = 
                    '<div class="loading">Failed to load API categories</div>';
            });
        
        function displayCategories(categories) {
            const container = document.getElementById('apiCategories');
            
            if (categories.length === 0) {
                container.innerHTML = '<div class="loading">No API categories available</div>';
                return;
            }
            
            container.innerHTML = categories.map(category => `
                <div class="category-card">
                    <div class="category-header">
                        <div class="category-icon">${category.icon || '📦'}</div>
                        <div class="category-title">
                            <h3>${category.name}</h3>
                            <p>${category.description}</p>
                        </div>
                    </div>
                    <div class="api-list">
                        ${(category.routes || []).map(route => `
                            <div class="api-item">
                                <div class="api-item-header">
                                    <span class="api-name">${route.name}</span>
                                    <code class="api-path">${route.path}</code>
                                </div>
                                <div class="api-description">${route.description}</div>
                                <div class="api-methods">
                                    ${(route.method || ['GET']).map(method => 
                                        `<span class="method-badge method-${method.toLowerCase()}">${method}</span>`
                                    ).join('')}
                                </div>
                            </div>
                        `).join('')}
                    </div>
                </div>
            `).join('');
        }
        
        function formatUptime(seconds) {
            if (!seconds) return '--';
            const hours = Math.floor(seconds / 3600);
            const minutes = Math.floor((seconds % 3600) / 60);
            
            if (hours > 24) {
                const days = Math.floor(hours / 24);
                return `$${days}d$$ {hours % 24}h`;
            }
            return `$${hours}h$$ {minutes}m`;
        }
    </script>
</body>
</html>
